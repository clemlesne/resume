doctype html
html(lang='fr-FR')
    block vars

    head
        meta(charset='UTF-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
        meta(name='viewport', content='width=device-width, initial-scale=1')

        title LESNÉ &middot; Résumé !{headerTitle}
        meta(name='author', content='Clément Lesné <clement@lesne.pro> (https://clement.lesne.pro/)')
        meta(name='description', content="Clément Lesné's resumé")

        link(rel='profile', href='http://gmpg.org/xfn/11')

        //@@css-critical

        //if lt IE 9
          script(src='https://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv-printshiv.min.js')

        script(type='application/javascript').
            var _global = {};

    body
        article.resume
            .resume__container
                include ../components/header

                .container.container--content
                    .content__container
                        .row
                            block content

                .container.container--footer(class=footerIsLeft ? 'container--footer--left' : 'container--footer--right')
                    if qrcodeVisible
                        span.container--footer__qrcode
                            span Mon profil
                            figure: img(src='img/qrcode.svg?v=@@version')
                    if footerVersionVisible
                        span
                            p v@@version

        span(style="display:none;")
            noscript: link(href='css/app.css?v=@@version', rel='stylesheet')

            script.
                function loadCSS(e, n, o, t) {
                    'use strict';
                    var d = window.document.createElement('link'),
                        i = n || window.document.getElementsByTagName('script')[0], r = window.document.styleSheets;
                    return d.rel = 'stylesheet', d.href = e, d.media = 'only x', t && (d.onload = t), i.parentNode.insertBefore(d, i), d.onloadcssdefined = function (e) {
                        for (var n, o = 0; o < r.length; o++) r[o].href && r[o].href === d.href && (n = !0);
                        n ? e() : setTimeout(function () {
                            d.onloadcssdefined(e)
                        })
                    }, d.onloadcssdefined(function () {
                        d.media = o || 'all'
                    }), d
                }
                loadCSS('css/app.css?v=@@version');

            script(src='js/app.js?v=@@version', charset='utf-8')

            script.
                WebFontConfig = {
                    google: {
                        families: ['Average:400', 'Roboto:400,400i', 'Kalam:400']
                    },
                    timeout: 5000,
                    events: false
                };

                (function (d) {
                    var wf = d.createElement('script'), s = d.scripts[0];
                    wf.src = 'https://ajax.googleapis.com/ajax/libs/webfont/1/webfont.js';
                    wf.async = true;
                    s.parentNode.insertBefore(wf, s);
                })(document);

            script.
                (function (i, s, o, g, r, a, m) {
                    i['GoogleAnalyticsObject'] = r;
                    i[r] = i[r] || function () {
                        (i[r].q = i[r].q || []).push(arguments)
                    }, i[r].l = 1 * new Date();
                    a = s.createElement(o), m = s.getElementsByTagName(o)[0];
                    a.async = 1;
                    a.src = g;
                    m.parentNode.insertBefore(a, m)
                })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');
                ga('create', _global.analytics.google   .id, 'auto');
                ga('send', 'pageview');